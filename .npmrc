# Some packages haven't updated their peerDependencies metadata for React 19
# yet. This setting prevents npm from failing the install over those
# (non-breaking) peer dependency warnings.
legacy-peer-deps=true
